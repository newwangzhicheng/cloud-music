export default class PlaylistCluaseModel {
  constructor(model) {
    this.title = model.title;
    this.songNumber = model.songNumber;
    this.src = model.src;
    this.isLove = model.isLove;
  }
}
